openapi: 3.1.0
info:
  title: Example service
  version: 1.1.0
paths:
  /v1/widgets:
    post:
      operationId: createWidget
      security: [{bearerAuth: []}]
      responses:
        '201':
          description: A widget was created.
