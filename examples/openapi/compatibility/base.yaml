openapi: 3.1.0
info:
  title: Example service
  version: 1.0.0
paths:
  /v1/widgets:
    get:
      operationId: listWidgets
      security: [{bearerAuth: []}]
      responses:
        '200':
          description: A list of widgets.
