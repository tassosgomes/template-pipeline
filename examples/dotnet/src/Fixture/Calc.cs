namespace Fixture;

/// <summary>Lógica trivial, só para haver algo real a testar e medir cobertura.</summary>
public static class Calc
{
    /// <summary>Devolve a soma de dois inteiros.</summary>
    public static int Soma(int a, int b) => a + b;
}
