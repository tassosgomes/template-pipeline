package com.exemplo;

/** Lógica trivial, só para haver algo real a testar e medir cobertura. */
public final class Calc {

    private Calc() {
    }

    /** Devolve a soma de dois inteiros. */
    public static int soma(int a, int b) {
        return a + b;
    }
}
