# Fixture de resolução .NET

Este diretório prova o contrato de `global.json` usado pelo `setup-toolchain`: SDK 10,
`rollForward: latestFeature` e `Microsoft.Testing.Platform`. Não é executado como uma das seis
pipelines porque não contém um projeto; `examples/dotnet` representa o caminho sem `global.json`.
